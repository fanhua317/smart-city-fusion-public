# 第八轮混合微调记录

生成时间：2026-06-06

## 方法

第八轮尝试把第四轮稳健候选 `r4_02_micro_edge_balanced43` 与第七轮高 AG/SF 候选 `r7_13_project_leader_struct` 混合：

- 低比例混合：`r7_13` 占 `15%-45%`
- 细节混合：从 `r7_13` 或 `r7_10` 转移少量高频
- 多源混合：加入 `r5_08` 和 `avg12` 降风险

## 结果

本轮没有得到超过 `r4_02` 的校准预测候选。

最佳第八轮候选：

- 候选：`05_r4_r7struct_detail_w18`
- 预测插队平均名次：`24.23`
- 预测指标：
  - `AG=5.597`
  - `Qabf=0.532`
  - `SD=40.70`
  - `SF=6.80`
  - `SSIM=0.742`
  - `Nabf=0.029`

对比：

- `r4_02_micro_edge_balanced43` 预测插队约 `22.15`
- `r7_13_project_leader_struct` 预测插队约 `23.69`

## 判断

第八轮不生成官方 `.dt`，不进入上传队列。它说明简单线性混合不能同时保住 `r4_02` 的结构优势和 `r7_13` 的 AG/SF 增益；后续需要真实上传反馈后再决定是否继续沿这个方向微调。

