# 第三轮上传队列

## 当前状态

GPU 远程电脑暂时关机，本轮在本机完成传统增强候选的官方 `.dt` 生成。

本轮关键修复：`Evaluation.exe` 在中文项目路径或残留 GUI 状态下不稳定，改用 ASCII 中转目录 `D:\fusion_eval` 后，第三轮候选已能正常生成官方 `.dt`。

## 已生成的第三轮 `.dt`

1. `10_source_edge_avg_low30`
   - 文件：`results/official_round3/10_source_edge_avg_low30/r3_10_source_edge_avg_low30.dt`
   - 本地候选排名：第 1
   - 特点：本地 `Qabf/AG/SF/CC` 综合最好，但 `Nabf` 相对激进。

2. `07_source_edge_vi030_ir006`
   - 文件：`results/official_round3/07_source_edge_vi030_ir006/r3_07_source_edge_vi030_ir006.dt`
   - 本地候选排名：第 2
   - 特点：比 `10` 更保守，结构略稳，适合作为第二个上传试探。

3. `09_source_edge_safe_qabf`
   - 文件：`results/official_round3/09_source_edge_safe_qabf/r3_09_source_edge_safe_qabf.dt`
   - 本地候选排名：第 3
   - 特点：Qabf 增强更谨慎，若官方反馈 `10/07` 的 `Nabf` 偏差，则优先传它。

## 推荐上传顺序

1. 先上传 `r3_10_source_edge_avg_low30.dt`。
2. 如果官方反馈 `Nabf` 或结构指标明显恶化，上传 `r3_09_source_edge_safe_qabf.dt`。
3. 如果官方反馈只是细节提升不足或排名接近，上传 `r3_07_source_edge_vi030_ir006.dt`。

## 注意

- 本地指标不能完全替代官方排行榜，最终仍以网页反馈为准。
- `.dt` 不纳入 Git，不手工编辑。
- 上传后把 13 项官方指标截图或 CSV 反馈回来，再继续做第四轮定向优化。
