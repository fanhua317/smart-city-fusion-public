# 第十一轮数据集自适应实验记录

## 数据集画像

- 脚本：`scripts/inspect_dataset.py`
- 输出：`results/dataset_profile.csv`
- 校验结果：IR/VI 文件名完全一致，数量 93，尺寸一致，可解码。
- 分组：`FLIR=40`，`DN_D=24`，`DN_N=16`，`GRAY_VI=13`。

## 候选池

- 数据集自适应候选：`results/candidates_round11_adaptive/`，共 64 个完整候选。
- 深度辅料候选：`results/candidates_round11_deep/`，检测到 `crossfuse_target`，派生 10 个轻量后处理版本；其他深度模型目录不存在，自动跳过。
- 每个候选均生成 `metrics_summary.json`，便于 selector 读取。

## 选择器

- 脚本：`scripts/generate_round11_selector.py`
- 校准报告：`results/round11_calibration_report.json`
- 候选池表：`results/round11_pool.csv`
- 最终分数表：`results/round11_local_scores.csv`
- 上传建议：`results/round11_eval/recommended_upload_order.md`

新增优化器：

- Beam search / coordinate descent
- Simulated annealing
- Group-aware selector

目标函数使用预测官方排名加硬约束惩罚，重点约束 `Nabf`、`SSIM`、`CC`、`SCD`、`Qabf`、`AG`、`SF`、`SD`。

## 最终候选

最终 10 个候选位于 `results/candidates_round11/`：

1. `r11_01_safe_rank`
2. `r11_02_attack_qabf`
3. `r11_03_attack_mi`
4. `r11_04_flir_boost`
5. `r11_05_night_boost`
6. `r11_06_deep_crossfuse_blend`
7. `r11_07_cddfuse_blend`
8. `r11_08_groupwise_best`
9. `r11_09_anneal_best`
10. `r11_10_beam_best`

每个最终目录只包含 93 张图片；manifest、validation、metrics 均保存在 `results/round11_eval/<candidate>/`。

## 校验

已对 10 个最终候选显式运行：

```powershell
py -3 scripts\validate_outputs.py
py -3 scripts\local_metrics.py
```

全部结果：`count=93`，`expected=93`，`problem_count=0`。

## 当前推荐上传

`r11_09_anneal_best` 和 `r11_08_groupwise_best` 未达到“比当前 03 至少好 1.5 rank”的触发条件，因此本轮不优先上传。

建议当天最多 5 个：

1. `r11_02_attack_qabf`：验证 Qabf 方向是否被官方认可。
2. `r11_01_safe_rank`：稳健保底。
3. `r11_07_cddfuse_blend`：当前 `PredInsertedRank` 最低，但因为无 CDDFuse 原始目录，实际为深度 fallback blend，用于验证深度辅料方向。
4. 保留一次给前三个官方反馈后的重新校准版本。

禁止项保持不变：不修改测试集，不修改 `.dt`，不把 `Evaluation.exe`、`target/`、`.dt`、大规模融合结果、模型仓库或权重提交到 GitHub。
